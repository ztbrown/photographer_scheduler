require_relative '../../app/interactors/create_session'

RSpec.describe CreateSession, "#create" do
  context "given a valid request model" do
  #  it "should create a new session when all values are provided" do
   #   session = CreateSession.new({rate: 100, wedding_date: Date.new, photographer_id: 1, customer_id: 2 }).execute
    #  expect(session).to_not be_nil
   # end
  end
end
